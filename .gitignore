# Build outputs
/bin/
/dist/
/tmp/
web/dist/*
!web/dist/.keep
web/node_modules/

# Go
*.test
*.out
/vendor/
# `go build ./cmd/<name>/...` drops the binary at repo root; ignore the
# common ones rather than fight `go build`'s defaults.
/gemba
/gemba-ask
/gemba-bridge
/gemba-mcp
/gemba-state
/gemba-bd-hook
/gen-core-types

# Frontend
web/.vite/
.vite/
node_modules/
testing/e2e/test-results/
testing/e2e/playwright-report/
testing/e2e/playwright/.cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Secrets (never commit)
.env
.env.local
*.pem
*.key
*.crt

# Audit log produced at runtime
~/.gemba/

# Gas Town (added by gt)
.runtime/
.claude/
.logs/
__pycache__/
state.json
CLAUDE.md
CLAUDE.local.md

# Rig-level beads config (machine-specific; storage is in Dolt server)
.beads/
.beads/
mail/
.DS_Store
.gitnexus
