## Conflict Detection Report

Generated by `gsd-doc-synthesizer` on 2026-05-21. Mode: new. Precedence: ADR > SPEC > PRD > DOC (with per-doc overrides honored).

Inputs analyzed: 3 docs (1 ADR LOCKED, 2 SPEC). Cross-ref cycle detection: clean. Confidence floor: all `high`. Unknown-low classifications: 0.

### BLOCKERS (0)

None. The single LOCKED ADR (`docs/process/gsd-beads-bridge.md`) governs a process domain (GSD↔Beads bridge) that does not contradict either SPEC's runtime/backend domain. No LOCKED-vs-LOCKED contradictions. No existing-context (merge-mode) checks performed — MODE=new.

### WARNINGS (0)

None. No PRDs were ingested, so no competing acceptance variants are possible from this set. The two SPECs do not overlap on a single requirement scope with divergent acceptance — they cover complementary surfaces (gemba-lite extends `OrchestrationPlaneAdaptor` with streaming/input verbs; containerized-sessions refactors `Backend` nouns and adds the security envelope).

### INFO (1)

[INFO] Auto-coexistence: backend-noun rename vs. adapter-verb extension
  Found: `docs/design/containerized-sessions.md` (SPEC, precedence 1) renames `internal/adapter/native/backend.Pane` → `Session` with a `Kind` field and grows `SpawnSpec` with container-specific optional fields.
  Found: `docs/design/gemba-lite.md` (SPEC, precedence 0) leaves the existing `Backend` interface in place and adds an optional `Streamable` extension plus three new `OrchestrationPlaneAdaptor` methods (`SendInput`, `ResizeSession`, `StreamSession`).
  Resolution: Coexistence — the two changes are orthogonal (one renames nouns at the backend layer, the other adds verbs at the adapter layer). Per per-doc precedence (gemba-lite = 0, containerized-sessions = 1), if any future overlap arises on the same symbol, gemba-lite's contract wins. Synthesized into `constraints.md` as G-1/G-2 (gemba-lite) and C-1 (containerized-sessions) without modification; downstream implementers should land the `Pane`→`Session` rename and the `Streamable` interface in the same refactor to avoid two churn windows.
  → No user action required. Flagged for transparency only.
