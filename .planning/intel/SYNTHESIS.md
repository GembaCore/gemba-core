# Synthesis Summary

Generated by `gsd-doc-synthesizer` on 2026-05-21. Mode: new. Single entry point for downstream consumers (`gsd-roadmapper`).

## Inputs

- **Docs synthesized:** 3
  - ADR (LOCKED): 1 — `docs/process/gsd-beads-bridge.md`
  - SPEC: 2 — `docs/design/gemba-lite.md` (precedence 0), `docs/design/containerized-sessions.md` (precedence 1)
  - PRD: 0
  - DOC: 0
- **Unknown/low-confidence:** 0
- **Cross-ref cycles:** none detected

## Outputs

| File | Contents | Count |
|---|---|---|
| `decisions.md` | LOCKED decisions extracted from ADR | 8 decisions (all LOCKED, all from `gsd-beads-bridge.md`) |
| `requirements.md` | Per-PRD requirements | 0 (no PRDs ingested — stub) |
| `constraints.md` | Per-SPEC constraints | 31 constraints (10 from gemba-lite as G-1..G-10; 21 from containerized-sessions as C-1..C-21) |
| `context.md` | DOC topics | 0 (no DOCs ingested — informational stub) |

## Conflicts

- **Blockers:** 0
- **Competing variants:** 0
- **Auto-resolved/informational:** 1 (noun-rename vs verb-extension coexistence — see INFO entry)

Full detail: `/Users/mikebengtson/Documents/GitHub/gemba/.planning/INGEST-CONFLICTS.md`

## Key locked decisions (for downstream routing)

1. GSD owns plan + executor; Beads owns tracker. No bidirectional sync daemon.
2. Bridge is a naming convention — every `PLAN.md` task carries `bead: <id>`.
3. gsd-executor lifecycle: claim → note (per atomic commit) → close.
4. Mapping: project root→Decision, ROADMAP entry→Milestone, phase→Epic, PLAN task→Story.
5. Reviews and verification logs are gsd artifacts, NOT bead rows.
6. Per-task atomic commits are bead `note` entries, NOT separate beads.
7. Cumulative labels mandatory on every bead (Beads does not auto-inherit).

## Key constraint clusters (for downstream routing)

- **gemba-lite (precedence 0):** new `OrchestrationPlaneAdaptor` verbs (`SendInput`/`ResizeSession`/`StreamSession`), `Streamable` backend extension, three HTTP endpoints (`/stream`, `/input`, `/status`), SPA three-pane workspace, 7-slice execution plan behind `?ws=1` flag.
- **containerized-sessions (precedence 1):** `Pane`→`Session` rename with `Kind` field, three transports (Docker local / `DOCKER_HOST=ssh` / pure SSH), security envelope defaults, secret injection via tmpfs (never env), orphan reaper, pack-ins (`native`/`containerized`/`gastown`), k8s-future-proofing audit checklist.

## Routing posture

STATUS: READY — safe to route to `gsd-roadmapper`. No blockers, no competing variants. One INFO entry is purely informational.
