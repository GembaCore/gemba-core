# Context (synthesized from DOCs)

Generated by `gsd-doc-synthesizer` on 2026-05-21.

**No DOC-class documents were classified in this ingest set.** All three inputs classified as ADR or SPEC.

## Background derived from ADR/SPEC framing (informational only — not authoritative)

- **Initiative:** gemba-lite — workable native (tmux-backed) dispatch release whose primary UX is a desktop-style session workspace. Root Decision bead: `gm-v01`.
- **Container epic:** gm-root.15, ratified 2026-04-24 (decision gm-nsdl/D9). Two pack-ins ride on top: `gemba-native` (default, tmux) and `gemba-containerized` (Docker sandbox).
- **Tracker integration model:** GSD↔Beads loosely coupled via PLAN.md `bead:` field convention. Beads is durable tracker; GSD owns plan + autonomous executor. See decisions.md D1–D8.

Authoritative content lives in `decisions.md` and `constraints.md`. This file exists so downstream consumers can detect "no DOC context ingested" via near-empty body rather than missing file.
