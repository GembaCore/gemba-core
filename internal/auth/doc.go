// Package auth implements authentication and authorization for the
// HTTP surface: token auth, TLS, and (post-v1) OIDC. Every code path
// here is security-sensitive; see gm-m2-auth formula.
package auth
