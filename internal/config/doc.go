// Package config holds server configuration types and the bind-policy
// check. This package is intentionally small and dependency-free so the
// bind policy can be tested in isolation and vendored without pulling in
// the HTTP stack.
package config
